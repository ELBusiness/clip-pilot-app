{
  "name": "perfect-season",
  "version": "0.1.0",
  "private": true,
  "description": "162-0 — an MLB roster-draft game. Spin for a franchise and a decade, draft thirteen legends, simulate all 162 games.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx tests/*.test.ts",
    "import:lahman": "tsx scripts/import-lahman.ts",
    "build:standalone": "node scripts/build-standalone.mjs",
    "build:assets": "node scripts/build-assets.mjs",
    "preview": "npx --yes serve out -l 3000"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "esbuild": "^0.28.2",
    "playwright": "^1.62.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "postcss": "8.5.16"
  }
}
